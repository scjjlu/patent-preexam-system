[metadata]
name = patent-preexam-system
version = 0.1.0

[options]
package_dir =
    = src
packages = find:
install_requires =
    pyyaml>=6.0
    lxml>=5.0
    pillow>=10.0

[options.packages.find]
where = src

[options.extras_require]
test =
    pytest>=8.0
